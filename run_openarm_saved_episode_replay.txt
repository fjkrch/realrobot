#!/usr/bin/env bash
set -euo pipefail

script_path="$(realpath "${BASH_SOURCE[0]}")"
repo_root="$(cd "$(dirname "${script_path}")" && pwd)"
exec "${repo_root}/docs/real/run_openarm_saved_episode_replay.txt" "$@"
