#!/usr/bin/env bash
set -euo pipefail

script_path="$(realpath "${BASH_SOURCE[0]}")"
repo_root="$(cd "$(dirname "${script_path}")" && pwd)"
exec "${repo_root}/docs/sim/run_default_openarm_mirror.txt" "$@"
