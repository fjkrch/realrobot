training:
  dataset: synthetic_smolvla/datasets/openarm_synth_v1
  checkpoint_dir: synthetic_smolvla/checkpoints/smolvla_openarm_synth_v1
  policy: smolvla
  device: cuda
  batch_size: 4
  steps: 3000
  run_mode: preflight_only

